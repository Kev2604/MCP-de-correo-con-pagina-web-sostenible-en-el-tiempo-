{
  "mcpServers": {
    "correo-mcp": {
      "command": "docker",
      "args": [
        "run",
        "--rm",
        "-i",
        "--env-file",
        "C:/Users/.env",
        "mcp-correo:latest"
      ],
      "cwd": "C:/mcp-correo"
    }
  }
}
